\documentclass{eptcs}
\usepackage{hyperref}
\usepackage{amsmath, amssymb}
\usepackage{stmaryrd}
\usepackage{tikzit}
\newcommand{\tikzroot}{../tikz}
\IfFileExists{\tikzroot/zxw_styles.tikzdefs}{}{\renewcommand{\tikzroot}{extended-abstract/../tikz}}
\newcommand{\refsroot}{../refs}
\IfFileExists{\refsroot.bib}{}{\renewcommand{\refsroot}{extended-abstract/../refs}}
\input{\tikzroot/zxw_styles.tikzdefs}
\input{\tikzroot/zxw_styles.tikzstyles}
\usepackage[english]{babel}
\usepackage{enumitem}

\newcommand{\Control}{\textsf{Ctrl}}
\newcommand{\Rule}[1]{\textsc{#1}}
\newcommand{\CSzero}{\Rule{CS0}}
\newcommand{\CScpy}{\Rule{CScpy}}
\newcommand{\CMzero}{\Rule{CM0}}
\newcommand{\CMcpy}{\Rule{CMcpy}}
\newcommand{\CMcom}{\Rule{CMcom}}
\newcommand{\csring}{\tilde{\mathcal{S}}_n}
\newcommand{\polyring}{\mathbb{C}[x_1,\ldots,x_n]/(x_1^2,\ldots,x_n^2)}
\newcommand{\lowerbox}[2][5]{\raisebox{-#1pt}{#2}}
\newcommand{\zspids}[1][0.4]{\begin{tikzpicture}[scale=#1]
  \begin{pgfonlayer}{nodelayer}
    \node [style=gn] (0) at (0, 0) {};
    \node [style=none] (1) at (0.5, -0.5) {};
    \node [style=none] (2) at (-0.5, -0.5) {};
    \node [style=none] (3) at (0, 0.75) {};
    \node [style=label] (4) at (0, -0.5) {$\ldots$};
  \end{pgfonlayer}
  \begin{pgfonlayer}{edgelayer}
    \draw (0) to (1.center);
    \draw (2.center) to (0);
    \draw (3.center) to (0);
  \end{pgfonlayer}
\end{tikzpicture}
}
\newcommand{\numberstate}[1][a]{\begin{tikzpicture}[scale=0.4]
  \begin{pgfonlayer}{nodelayer}
    \node [style=gbox] (0) at (0, 0) {$#1$};
    \node [style=none] (1) at (0, 1) {};
  \end{pgfonlayer}
  \begin{pgfonlayer}{edgelayer}
    \draw (1.center) to (0);
  \end{pgfonlayer}
\end{tikzpicture}
}
\setlength{\textfloatsep}{8pt plus 1pt minus 2pt}
\setlength{\floatsep}{7pt plus 1pt minus 1pt}
\setlength{\intextsep}{7pt plus 1pt minus 1pt}
\setlength{\abovedisplayskip}{4pt}
\setlength{\belowdisplayskip}{4pt}
\setlength{\abovedisplayshortskip}{2pt}
\setlength{\belowdisplayshortskip}{2pt}
\setlist[itemize]{leftmargin=*,nosep}
\setcounter{secnumdepth}{0}

\title{Algebraic Structure of Quantum Controlled States and Operators}
\author{Edwin Agnew \and Lia Yeh \and Richie Yeung}
\def\titlerunning{PolyZXW Extended Abstract}
\def\authorrunning{Agnew, Yeh, Yeung}

\begin{document}
\maketitle

\begin{abstract}
We show that controlled diagrams in the ZXW-calculus admit rich algebraic structure. Controlled states form a commutative ring isomorphic to multilinear polynomials, while controlled square matrices form a non-commutative ring. These properties supply new rewrite rules that make factorisation of arbitrary qubit Hamiltonians possible inside a single graphical calculus. The higher-order map $\Control$ recovers familiar controlled gates and composes cleanly, enabling multi-control and black-box Hamiltonian reasoning without leaving the diagrammatic setting.
\end{abstract}

Quantum control is a universal primitive that underlies block encodings, LCUs and Hamiltonian simulation. Yet graphical calculi typically treat control as a gadget rather than a first-class algebraic object. We work in the qubit ZXW-calculus (complete for finite-dimensional linear maps) and add controlled states and controlled square matrices as generators. The goal is to characterise their algebraic laws and exploit them for equational reasoning.
These rewrite rules already support Hamiltonian-level reasoning in practice: they are used for Hamiltonian summation/exponentiation~\cite{shaikh2022sum}, and the copy rule \CMcpy\ found in this work has been used to derive general forms of two-body fermionic Hamiltonians under arbitrary linear encodings~\cite{mcdowallrose2025f2q}, directly relevant to electronic-structure mappings.
As a consequence, completeness for matrix-valued polynomials gives a purely diagrammatic route to factorising arbitrary qubit Hamiltonians (see Fig.~\ref{fig:factorization-proof}), including settings with black-box matrix terms. The same framework unifies ordinary controlled gates, multi-control constructions, and algebraic manipulation of controlled states in one calculus. Interpreting controlled states as multilinear polynomials also exposes direct links to classical algebraic tooling for optimization and structural analysis.

\paragraph{Controlled generators (Def.~2.1, 2.2).} We reproduce the two defining equations exactly to fix notation and semantics before stating the main algebraic results.
\par
\noindent
\begin{minipage}[t]{0.49\linewidth}
\vspace{0pt}
\begin{equation}
  \scalebox{0.76}{$\left\llbracket \quad \tikzfig{\tikzroot/con/c_sq_def} \;\;\; \right\rrbracket ~=~ \bra{0} \otimes I + \bra{1} \otimes M ~=~ \begin{bmatrix} I & M \end{bmatrix}$}
  \label{eq:def-csq}
\end{equation}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\linewidth}
\vspace{0pt}
\begin{equation}
 \raisebox{0.6ex}{\scalebox{0.76}{$\left\llbracket \;\;\; \tikzfig{\tikzroot/con/c_state_def} \;\;\; \right\rrbracket ~=~ \ket{\vec{0}}\bra{0} + \ket{\psi}\bra{1} = \left[\ket{\vec{0}} \quad \ket{\psi}\right]$}}
\label{eq:def-cstate}
\end{equation}
\end{minipage}

\subsection{Ring structure of controlled states}
With operations $\boxplus$ and $\boxtimes$, controlled $n$-partite states form a commutative ring. Operationally, additive identity/inverse, associativity/commutativity, and distributivity are derived natively through the axioms of the calculus.

\subsection{Ring structure of controlled square matrices}
Controlled square matrices form a non-commutative ring under controlled addition and composition. The two base equations from Eq.~3.2 are:
\begin{equation}
  \tikzfig{\tikzroot/con/c_sq0}
  \qquad
  \tikzfig{\tikzroot/con/c_sq1}
  \label{eq:csq-pair}
\end{equation}
These identities fix the inactive and active sectors. Together with copy and sector-wise commutation (Fig.~\ref{fig:ctrl-ring}), they permit duplication and reordering of terms in disjoint sectors entirely inside the calculus.

\subsection{Polynomial semantics and complete normal form}
Arithmetic ZXW diagrams admit a canonical polynormal form (PNF), shown in Fig.~\ref{fig:pnf}. The key result is the following isomorphism:
\[
\tilde{\mathcal{S}}_n \simeq \polyring.
\]
So controlled states correspond exactly to multilinear polynomials. Equality checking reduces to rewriting both sides to PNF and comparing coefficients. This builds on state-normal-form completeness from ZW/ZX~\cite{hadzihasanovic2017thesis,Hadzihasanovic2018zwzxcomplete}, and goes beyond prior Cartesian-Distributive analyses where explicit rule-level derivation was given only for Boolean circuits~\cite{wilson2021revderbool,wilson2023diffpolycirc}.

First, we shall define the map $\phi_n: \polyring \to \csring$ before proving it induces an isomorphism. $\phi_n$ is defined to map an arbitrary polynomial $p(x_1, ..., x_n) = a_0 + a_1x_n + ... + a_{2^n-1}x_1x_2...x_n$ to the PNF in Fig.~\ref{fig:pnf}.

The proof that $\phi_n$ is a homomorphism follows from controlled addition/multiplication rewrites, and bijectivity follows from uniqueness of the PNF coefficients.


\begin{figure}[t]
  \centering
  \tikzfig{\tikzroot/poly/pnf}
  \vspace{0.2em}
  {\small
  $
  ~=~
  \begin{bmatrix}
    1 & a_0 \\
    0 & a_1 \\
    \vdots & \vdots \\
    0 & a_{2^n-1}
  \end{bmatrix}$}
  \caption{Polynormal form for controlled states in the arithmetic fragment of the ZW-calculus.}
  \label{fig:pnf}
\end{figure}

\begin{figure}[t]
  \centering
  \renewcommand{\arraystretch}{1.25}
  \begin{tabular}{|p{0.45\textwidth}|p{0.45\textwidth}|}
    \hline
    \multicolumn{2}{|c|}{\textbf{Controlled Ring Rules (subset of Fig.~1)}} \\
    \hline
    \begin{minipage}{\linewidth}
      \vspace{-1.2em}
      \begin{gather*}
        \tikzfig{\tikzroot/con/c_state0}\\[-0.4em]
        \CSzero\\
        \scalebox{0.9}{\tikzfig{\tikzroot/con/cs_copy}}\\[-0.4em]
        \CScpy
      \end{gather*}
    \end{minipage} &
    \begin{minipage}{\linewidth}
      \vspace{-1.2em}
      \begin{gather*}
        \scalebox{0.9}{\tikzfig{\tikzroot/con/c_sq0}}\\[-0.4em]
        \CMzero\\
        \scalebox{0.85}{\tikzfig{\tikzroot/con/csq_copy}}\\[-0.4em]
        \CMcpy\\
        \scalebox{0.85}{\tikzfig{\tikzroot/con/csq_add_comm_statement}}\\[-0.4em]
        \CMcom
      \end{gather*}
    \end{minipage}\\
    \hline
  \end{tabular}
  \caption{Controlled ring rules used below: identities on $|0\rangle$, copy lemmas, and disjoint-sector commutation. Adding the ZX, ZW, and ZXW Rules here suffice for completeness of arithmetic diagrams (Theorem 5.1), the ring of controlled states (Theorem 5.2) and the ring of controlled square matrices (Theorem 6.1).}
  \label{fig:ctrl-ring}
\end{figure}

\subsection{Completeness for matrix-valued polynomials}
Replacing scalar indeterminates in PNF by controlled square matrices yields multivariate polynomials over same-size matrices. The same rewrite fragment remains complete in this lifted setting, enabling normalization and factorization with black-box matrix terms.

\subsection{Higher-order control map}
The map $\Control$ sends a matrix to its controlled form and preserves sequential and parallel composition. Iterating $\Control$ recovers standard multi-control gates (e.g., Toffoli from controlled-controlled-$X$), connecting circuit-level control to the rewrite theory above.

\paragraph{Ring and PNF summary.}
Let \(\tilde{\mathcal{S}}_n\) be the set of all controlled \(n\)-partite states.
We show that \((\tilde{\mathcal{S}}_n,\boxplus,\boxtimes)\) defines a commutative ring:
\begin{itemize}
\item \(\boxplus\) gives an Abelian group
\item \(\boxtimes\) gives a commutative monoid
\item \(\boxtimes\) distributes over \(\boxplus\)
\end{itemize}

Moreover, \(\tilde{\mathcal{S}}_n\) is a ring isomorphism of multilinear polynomials \(\mathbb{C}[x_1,\ldots,x_n]/(x_1^2,\ldots,x_n^2)\). Likewise, let \(\tilde{M}_n\) be the set of all controlled square matrices on \(n\) qubits. We show that \((\tilde{M}_n,+,\times)\) forms a non-commutative ring.

These structures support Polynomial Normal Form: higher-order generators are not repeated, and control structure is arranged canonically so coefficients of \(p(x_1,\ldots,x_n)\) appear in the standard PNF ordering (Fig.~\ref{fig:pnf}).

\subsection{Diagrammatic Verification of Ring Requirements}
\begingroup
\small
\paragraph{(1-2) Definitions of addition and multiplication.}
The left diagram defines weighted controlled addition \(\widetilde{\sum_i c_iM_i}\) by control-branching, and the right diagram defines controlled multiplication \(\widetilde{\prod_i M_i}\) by serial composition in the active sector.
\[
\scalebox{0.74}{\tikzfig{\tikzroot/con/csq_sum}}
\qquad\qquad\qquad
\scalebox{0.74}{\tikzfig{\tikzroot/con/csq_prod}}
\]

\paragraph{(3) Definition of scalar multiplication.}
For \(c\in\mathbb{C}\), set \(a\odot\tilde M:=\widetilde{aM}\), the one-branch instance of (2). $\lowerbox{\numberstate}$

\paragraph{(4) Commutativity of addition.}
This is \CMcom: swapping additive branches preserves the same controlled sum.

\paragraph{(5) Additive identity and additive inverse.}
Using (1), \(\tilde M\boxplus\widetilde{0}=\tilde M\), and with \((-\tilde M):=(-1)\odot\tilde M\), \(\tilde M\boxplus(-\tilde M)=\widetilde{0}\).

\paragraph{(6) Associativity of addition.}
Regrouping the same addition wiring from (1) gives
\((\tilde M_1\boxplus\tilde M_2)\boxplus\tilde M_3=\tilde M_1\boxplus(\tilde M_2\boxplus\tilde M_3)\).

\paragraph{(7) Multiplicative identity.}
By \CMzero\ with \(M=I\), the unit is \(\widetilde{I}\), with
\(\widetilde{I}\boxtimes\tilde M=\tilde M=\tilde M\boxtimes\widetilde{I}\).

\paragraph{(8) Associativity of multiplication.}
Regrouping the product wiring from (2) gives
\((\tilde M_1\boxtimes\tilde M_2)\boxtimes\tilde M_3=\tilde M_1\boxtimes(\tilde M_2\boxtimes\tilde M_3)\).

\paragraph{(9) Left and right distributivity.}
Control copying (\CMcpy) yields left distributivity; the mirrored rewrite yields right distributivity.
\[\scalebox{0.62}{\tikzfig{\tikzroot/con/csq_dist_statement}}\].

\paragraph{(10) Multiplicative inverse (when defined).}
Not a ring axiom. If \(M\) is invertible, then
\(\tilde M\boxtimes\widetilde{M^{-1}}=\widetilde{I}=\widetilde{M^{-1}}\boxtimes\tilde M\).
\endgroup

\begin{figure}[t]
  \centering
  \[
  \resizebox{0.50\linewidth}{!}{$\widetilde{aI + bA^2 + cBA}\stackrel{\CMcpy,\Rule{BZW}}{=}\widetilde{aI + bA\!\cdot\!A + cB\!\cdot\!A}\stackrel{\CMcom}{=}\widetilde{aI + (bA+cB)A}$}
  \]
  \caption{Compact one-line factorization proof (Figure 3), preserving the same rewrite steps.}
  \label{fig:factorization-proof}
\end{figure}

\begingroup
\footnotesize
\bibliographystyle{abbrv}
\bibliography{\refsroot}
\endgroup

\end{document}
